import 'package:flutter/material.dart';

class TripItemWidget extends StatelessWidget {
  const TripItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
